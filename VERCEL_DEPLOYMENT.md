# 🚀 Vercel Deployment Guide для Admin App

## 📋 Оновлення для використання @reelo/i18n з GitHub

### ✅ Що було зроблено:

1. **Оновлено package.json** - замінив `workspace:*` на GitHub репозиторій
2. **Додано скрипт копіювання** - `scripts/copy-locales.js` копіює переклади після install
3. **Створено vercel.json** - налаштування для monorepo на Vercel

## 🔧 Налаштування Vercel

### Варіант 1: Через Vercel Dashboard (Рекомендовано)

1. **Відкрийте Vercel Dashboard**: https://vercel.com/dashboard
2. **Додайте новий проект** або відкрийте існуючий
3. **Підключіть GitHub репозиторій**
4. **Налаштуйте наступні параметри:**

   ```
   Framework Preset: Vite
   Root Directory: apps/admin
   Build Command: pnpm build
   Output Directory: dist
   Install Command: pnpm install
   ```

5. **Додайте Environment Variables** (якщо потрібно):
   ```
   API_BASE_URL=your-api-url
   ```

6. **Deploy!**

### Варіант 2: Через Vercel CLI

```bash
# Встановити Vercel CLI
npm i -g vercel

# З кореня monorepo
cd apps/admin

# Deploy
vercel

# Або для production
vercel --prod
```

## 📝 Важливі моменти

### 1. Monorepo налаштування

Vercel автоматично визначить monorepo, але переконайтеся:
- ✅ Root Directory встановлено на `apps/admin`
- ✅ Build Command: `pnpm build`
- ✅ Install Command: `pnpm install`

### 2. Копіювання перекладів

Скрипт `postinstall` автоматично копіює переклади з `node_modules/@reelo/i18n/locales` в `public/locales` після встановлення залежностей.

Якщо це не працює, можна додати в build скрипт:
```json
{
  "build": "node scripts/copy-locales.js && tsc -b && vite build"
}
```

### 3. Environment Variables

Додайте в Vercel Dashboard → Settings → Environment Variables:
- `API_BASE_URL` (якщо використовується)

### 4. GitHub Token (якщо потрібно)

Якщо репозиторій приватний, додайте GitHub token:
1. Vercel Dashboard → Settings → Git
2. Додайте GitHub token з правами `repo`

## 🔍 Перевірка після деплою

1. **Перевірте, чи переклади завантажуються:**
   - Відкрийте DevTools → Network
   - Перевірте запити до `/locales/en/common.json`, `/locales/uk/menu.json` тощо

2. **Перевірте консоль на помилки:**
   - Не повинно бути помилок завантаження перекладів

3. **Перевірте різні мови:**
   - Перемкнути мову в додатку
   - Переконатися, що переклади змінюються

## 🐛 Troubleshooting

### Проблема: Переклади не завантажуються

**Рішення:**
1. Перевірте, чи `public/locales/` містить файли після build
2. Перевірте build logs в Vercel - чи виконується `postinstall`
3. Додайте явне копіювання в build скрипт

### Проблема: Build fails з помилкою про @reelo/i18n

**Рішення:**
1. Перевірте, чи репозиторій публічний або додайте GitHub token
2. Перевірте, чи правильний URL репозиторію в package.json

### Проблема: TypeScript помилки

**Рішення:**
1. Переконайтеся, що `tsconfig.app.json` extends `tsconfig.base.json`
2. Перевірте, чи paths правильно налаштовані

## ✅ Чеклист перед деплоєм

- [ ] Оновлено `package.json` з GitHub URL
- [ ] Створено `vercel.json` (якщо потрібно)
- [ ] Додано `scripts/copy-locales.js`
- [ ] Перевірено, що `postinstall` скрипт працює локально
- [ ] Налаштовано Root Directory в Vercel
- [ ] Додано Environment Variables (якщо потрібно)
- [ ] Перевірено build локально: `pnpm build`

## 🎉 Готово!

Після налаштування ваш admin app буде:
- ✅ Використовувати @reelo/i18n з GitHub
- ✅ Автоматично копіювати переклади під час build
- ✅ Працювати на Vercel з правильними перекладами

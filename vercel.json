{
  "buildCommand": "echo '🚀 Starting build...' && pwd && ls -la package.json && pnpm build",
  "outputDirectory": "dist",
  "installCommand": "echo '📦 Installing dependencies from monorepo root...' && cd ../.. && pnpm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

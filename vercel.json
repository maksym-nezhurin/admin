{
  "buildCommand": "echo '🚀 Starting build...' && echo '📂 Current directory:' && pwd && echo '📋 Running build script...' && pnpm build",
  "outputDirectory": "dist",
  "installCommand": "echo '📦 Installing dependencies from monorepo root...' && cd ../.. && pnpm install && cd apps/admin",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

# 🔧 Vercel Troubleshooting Guide

## Проблема: Build зависає після "🚀 Starting build..."

### ✅ Рішення 1: Перевірте Root Directory

**В Vercel Dashboard:**
1. Settings → General
2. **Root Directory** має бути: `apps/admin`
3. Збережіть зміни

### ✅ Рішення 2: Спрощені команди

Якщо Root Directory встановлено правильно, команди мають бути простішими:

**vercel.json:**
```json
{
  "buildCommand": "pnpm build",
  "installCommand": "cd ../.. && pnpm install && cd apps/admin"
}
```

### ✅ Рішення 3: Використовуйте Vercel CLI для тестування

```bash
# З кореня monorepo
cd apps/admin
vercel build
```

Це покаже, що саме не працює локально.

### ✅ Рішення 4: Альтернативний підхід - без vercel.json

Видаліть `vercel.json` і налаштуйте все в Dashboard:

**Settings → General:**
- Root Directory: `apps/admin`
- Build Command: `pnpm build`
- Output Directory: `dist`
- Install Command: `cd ../.. && pnpm install && cd apps/admin`

### 🐛 Діагностика

Якщо build все ще не працює, перевірте логи:

1. **Шукайте помилки після "🚀 Starting build..."**
2. **Перевірте, чи виконується `pnpm build`**
3. **Перевірте, чи є помилки в `copy-locales.js`**

### 📋 Чеклист

- [ ] Root Directory встановлено на `apps/admin`
- [ ] `vercel.json` містить правильні команди
- [ ] `package.json` має скрипт `build`
- [ ] Залежності встановлюються успішно
- [ ] `@reelo/i18n` встановлюється з GitHub

# 🎯 Навігація з розділами та розкривними підпунктами

## 📋 Огляд

Створено нову навігацію `CollapsibleDashboardNav` з:
- ✅ Розділи меню (категорії)
- ✅ Розкривні підпункти
- ✅ Перший розділ розкритий за замовчуванням
- ✅ Бейджі для важливих розділів
- ✅ Фільтрація за рівнем доступу

---

## 🎨 Структура меню

### **Розділи:**
1. **Основне** (розкрито за замовчуванням)
   - Головна панель
   - Профіль
   - Налаштування

2. **Контент**
   - Оголошення

3. **Інструменти** (PRO)
   - Скрапер

4. **Адмін панель** (ADMIN)
   - Користувачі
   - Аудит
   - Приклади ролей

5. **Система** (SUPER_ADMIN)
   - Системні налаштування
   - Логи системи
   - Моніторинг

---

## ⚙️ Конфігурація

### **Додати новий розділ:**
```tsx
{
  label: 'Новий розділ',
  requiredLevel: 60, // MANAGER+
  badge: 'NEW',
  defaultOpen: false, // Закритий за замовчуванням
  items: [
    { label: 'menu.new_item', path: 'new-item', requiredLevel: 60 },
  ]
}
```

### **Додати підпункт:**
```tsx
{
  label: 'Існуючий розділ',
  items: [
    // ... існуючі пункти
    { 
      label: 'Новий підпункт', 
      path: 'new-subitem', 
      requiredLevel: 80,
      badge: 'HOT'
    },
  ]
}
```

### **Налаштувати розділ за замовчуванням:**
```tsx
{
  label: 'Основне',
  defaultOpen: true, // Розкрито за замовчуванням
  items: [...]
}
```

---

## 🎯 Функції

### **1. Автоматичне розкриття активних розділів:**
```tsx
// Розділ автоматично розкривається, якщо один з підпунктів активний
const hasActiveItem = section.items.some(item => 
  item.path && isActive(item.path)
);
```

### **2. Фільтрація за рівнем доступу:**
```tsx
// Розділ приховується, якщо користувач не має доступу
const availableSections = MENU_SECTIONS.filter(section => 
  !section.requiredLevel || roleLevel?.level >= section.requiredLevel
);
```

### **3. Візуальні індикатори:**
```tsx
// Бейджі для розділів
{ badge: 'ADMIN' } // Показує бейдж біля назви розділу

// Бейджі для пунктів
{ badge: 'HOT' } // Показує бейдж біля пункту меню
```

---

## 🔄 Альтернативні варіанти

### **1. Прості розділи (без акордеону):**
```tsx
import { SectionedDashboardNav } from '../components/CollapsibleDashboardNav';

// Замість CollapsibleDashboardNav
<SectionedDashboardNav />
```

### **2. Кастомізація стилів:**
```tsx
<Accordion 
  styles={{
    item: {
      border: 'none',
      borderRadius: 0,
    },
    control: {
      padding: '8px 12px',
      '&:hover': {
        backgroundColor: '#f8f9fa',
      },
    },
  }}
>
```

---

## 🎨 Приклади використання

### **Створити розділ для звітів:**
```tsx
{
  label: 'Звіти',
  requiredLevel: 60,
  icon: <IconChartBar size={16} />,
  items: [
    { label: 'Звіт по користувачах', path: 'reports/users' },
    { label: 'Звіт по системі', path: 'reports/system' },
    { label: 'Фінансовий звіт', path: 'reports/financial', requiredLevel: 80 },
  ]
}
```

### **Створити розділ з іконками:**
```tsx
{
  label: 'Налаштування',
  icon: <IconSettings size={16} />,
  items: [
    { label: 'Профіль', path: 'profile', icon: <IconUser size={14} /> },
    { label: 'Безпека', path: 'security', icon: <IconLock size={14} /> },
  ]
}
```

---

## 🔧 Налаштування

### **Змінити поведінку розкриття:**
```tsx
// Всі розділи розкриті за замовчуванням
<Accordion multiple defaultValue={MENU_SECTIONS.map(s => s.label)}>

// Жоден розділ не розкритий
<Accordion multiple defaultValue={[]}>

// Тільки перший розділ розкритий
<Accordion multiple defaultValue={[MENU_SECTIONS[0].label]}>
```

### **Змінити відступи:**
```tsx
// Збільшити відступ для підпунктів
<NavLink
  pl={30} // Замість 20
  // ...
/>
```

---

## 📱 Мобільна версія

Навігація автоматично адаптується для мобільних пристроїв:
- Використовує `Drawer` замість `Navbar`
- Зберігає функціонал розкриття
- Оптимізовані розміри для сенсорних екранів

---

## 🎯 Результат

**✅ Переваги нової навігації:**
- **Організація:** Логічні групи пунктів меню
- **Економія місця:** Розкривні розділи
- **Інтуїтивність:** Зрозуміла структура
- **Гнучкість:** Легко додавати нові розділи
- **Доступність:** Фільтрація за ролями
- **Візуальні індикатори:** Бейджі та іконки

**🎯 Готово до використання!**

export interface ICar {
  id: string,
  ownerId: string,
  complectation: string,
  engine: string,
  model: string,
  type: string,
}

export interface IUser {
    id: string;
    name: string;
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    role: string;
    createdAt: string;
    updatedAt: string;
  }